Drop files here to add stores to the watchlist without triage (tagged source=manual):

- `.txt` or `.csv` with one domain or URL per line, or
- a CSV export (for example from gethookd) that has a column named domain / website / url / landing / link.

Processed files are moved to `done/`. Or use `python tracker.py radar-add <domain or url> ...`.
